// function definition with parameters
get_value_plus_one = value <- int > int {
  // return command with expression
  return value + 1;
}

// function definition without parameters
main => int {
  // variable declaration
  int var_a <= 2;
  int var_b, var_c <= 3;
  float var_d <= 4.5;
  float var_e;

  // variable assignment with expression
  var_b = !var_a * -var_c + var_d % var_a;

  // variable assignment with forced precedence
  var_e = (!var_a | (-var_b & (var_c == (var_d != 4.5))));

  // flow control with if and else
  if (var_b >= 5) {
    var_d = var_d * 2;
  } else {
    var_d = var_d / 2;
  };

  // flow control with if
  if (var_b >= 5) {
    var_d = 50;
  };

  // flow control with nested ifs and elses
  if (var_b > 2) {
    if (var_c > 2) {
      var_d = 100;
    } else {
      var_d = 200;
    };
  } else {
    var_d = 300;
  };

  // flow control with while
  while (var_d <= 50) {
    var_d = get_value_plus_one(var_d);
  };

  // return command with literal
  return 0;
}